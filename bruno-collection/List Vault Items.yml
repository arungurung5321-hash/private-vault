info:
  name: List Vault Items
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/vault/items"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
