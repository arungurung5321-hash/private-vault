info:
  name: Delete Item
  type: http
  seq: 8

http:
  method: DELETE
  url: "{{baseUrl}}/vault/items/{{itemId}}"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
