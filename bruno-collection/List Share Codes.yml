info:
  name: List Share Codes
  type: http
  seq: 12

http:
  method: GET
  url: "{{baseUrl}}/share/items/{{mediaItemId}}/shares"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
