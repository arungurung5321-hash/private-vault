info:
  name: Vault Stats
  type: http
  seq: 9

http:
  method: GET
  url: "{{baseUrl}}/vault/stats"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
