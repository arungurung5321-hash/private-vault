info:
  name: Vault Trash
  type: http
  seq: 10

http:
  method: GET
  url: "{{baseUrl}}/vault/trash"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
