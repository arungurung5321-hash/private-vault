info:
  name: Pending Share Requests
  type: http
  seq: 14

http:
  method: GET
  url: "{{baseUrl}}/share/pending"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
