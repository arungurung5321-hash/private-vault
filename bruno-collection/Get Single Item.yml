info:
  name: Get Single Item
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseUrl}}/vault/items/{{itemId}}"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
