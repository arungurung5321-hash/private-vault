info:
  name: List Media Files
  type: http
  seq: 15

http:
  method: GET
  url: "{{baseUrl}}/media/{{mediaItemId}}/files"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
